import "package:fling_units/fling_units.dart";

part "package:fling_units/src/generated/prefix.dart";

/// SI prefix "quecto".
const MeasurementPrefix quecto = MeasurementPrefix(1e-30, name: 'q');

/// SI prefix "ronto".
const MeasurementPrefix ronto = MeasurementPrefix(1e-27, name: 'r');

/// SI prefix "yocto".
const MeasurementPrefix yocto = MeasurementPrefix(1e-24, name: 'y');

/// SI prefix "zepto".
const MeasurementPrefix zepto = MeasurementPrefix(1e-21, name: 'z');

/// SI prefix "atto".
const MeasurementPrefix atto = MeasurementPrefix(1e-18, name: 'a');

/// SI prefix "femto".
const MeasurementPrefix femto = MeasurementPrefix(1e-15, name: 'f');

/// SI prefix "pico".
const MeasurementPrefix pico = MeasurementPrefix(1e-12, name: 'p');

/// SI prefix "nano".
const MeasurementPrefix nano = MeasurementPrefix(1e-9, name: 'n');

/// SI prefix "micro".
const MeasurementPrefix micro = MeasurementPrefix(1e-6, name: 'μ');

/// SI prefix "milli".
const MeasurementPrefix milli = MeasurementPrefix(1e-3, name: 'm');

/// SI prefix "centi".
const MeasurementPrefix centi = MeasurementPrefix(1e-2, name: 'c');

/// SI prefix "deci".
const MeasurementPrefix deci = MeasurementPrefix(1e-1, name: 'd');

/// SI prefix "deka".
const MeasurementPrefix deka = MeasurementPrefix(1e1, name: 'da');

/// SI prefix "hecto".
const MeasurementPrefix hecto = MeasurementPrefix(1e2, name: 'h');

/// SI prefix "kilo".
const MeasurementPrefix kilo = MeasurementPrefix(1e3, name: 'k');

/// SI prefix "mega".
const MeasurementPrefix mega = MeasurementPrefix(1e6, name: 'M');

/// SI prefix "giga".
const MeasurementPrefix giga = MeasurementPrefix(1e9, name: 'G');

/// SI prefix "tera".
const MeasurementPrefix tera = MeasurementPrefix(1e12, name: 'T');

/// SI prefix "peta".
const MeasurementPrefix peta = MeasurementPrefix(1e15, name: 'P');

/// SI prefix "exa".
const MeasurementPrefix exa = MeasurementPrefix(1e18, name: 'E');

/// SI prefix "zetta".
const MeasurementPrefix zetta = MeasurementPrefix(1e21, name: 'Z');

/// SI prefix "yotta".
const MeasurementPrefix yotta = MeasurementPrefix(1e24, name: 'Y');

/// SI prefix "ronna".
const MeasurementPrefix ronna = MeasurementPrefix(1e27, name: 'R');

/// SI prefix "quetta".
const MeasurementPrefix quetta = MeasurementPrefix(1e30, name: 'Q');
